// Importaciones por defecto
import React from 'react';
// Importaciones de componentes
import Results from '../../components/results/Results';

/**
 * ? Componente que contiene la Pagina donde se muestran los resultados de busqueda
 * @returns Devuelve la pagina que renderizará los resultados de la busqueda general
 */
function ResultsPage() {
  return (
    <Results/>
  )
}

export default ResultsPage